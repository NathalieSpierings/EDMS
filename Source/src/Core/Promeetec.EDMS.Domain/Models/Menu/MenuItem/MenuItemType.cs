namespace Promeetec.EDMS.Domain.Models.Menu.MenuItem;

public enum MenuItemType
{
    // The url will be set to 'javascript' and an attribute with the load-url will be set so it can be loaded true an ajax call.
    Ajax = 0,

    Link = 1,
}